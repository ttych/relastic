# frozen_string_literal: true

RSpec.describe Relastic do
  it "has a version number" do
    expect(Relastic::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
